  notify:
    runs-on: ubuntu-latest
    needs: [deploy]
    if: ${{ failure() }}
    steps:
      - name: Notify failure (placeholder)
        run: echo "Workflow failed; add Slack webhook/logic here."
